# Backend API base URL (needed if not using Vite dev proxy)
# For `npm run dev`: proxy in vite.config.ts forwards /api => localhost:3000
# Set for production or when pointing at remote API
VITE_API_BASE_URL=http://localhost:3000
